# Meta strategy

Meta updates store the untouched raw score matrix, antisymmetrized solver matrix, games, standard errors, probabilities, estimated value, convergence gap, and up to ten non-transitive cycles. Required Champions and exploiters are retained; remaining slots use hash uniqueness and diversity selection.
